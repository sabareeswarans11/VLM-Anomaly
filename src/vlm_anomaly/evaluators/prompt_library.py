"""YAML prompt library loader. Implemented in task 06.

Loads ``prompts/*.yaml`` and renders templates with context variables.
Both the Python toolkit and the iOS app consume the same YAML files.
"""

from __future__ import annotations
